
- @entries_by_category.each do |category, entries|

  %br
  =link_to category[1], entries_category_path(category[0])
  %ul
  - entries.each_with_index do |entry, i|

  -#Limit to the most recent 6 entries
  - break if i == 5
    %li
      =link_to image_tag(entry.image_url.to_s, :class =>"img-polaroid", :size => "100x100"), entry

    %br
  =link_to "More...", entries_category_path(category[0]), method: "get", :class => "btn btn-small"
  %br